import React from "react";
import { Table } from "antd";

// export default function List({ list, users}) {
//   const columns = []
//   return <Table pagination={false} column={} dataSource={list} />
// }
